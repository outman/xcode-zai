services:
  xcode-zai:
    image: oven/bun:canary
    container_name: xcode-zai
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "9595:3000"
    command: ["bun", "run", "src/index.ts"]
    restart: unless-stopped
    env_file:
      - .env
